AIRFLOW_UID=50000
_PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-databricks boto3 requests dbt-core dbt-databricks

# Used by analytics/cluster_trips.py to connect to the Databricks SQL
# warehouse directly (not via dbt) -- same credentials as ~/.dbt/profiles.yml.
DATABRICKS_HOST=
DATABRICKS_HTTP_PATH=
DATABRICKS_TOKEN=
